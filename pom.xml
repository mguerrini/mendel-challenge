<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.mendel</groupId>
    <artifactId>transaction-service</artifactId>
    <version>1.0.0</version>
    <name>transaction-service</name>
    <description>Mendel Java Code Challenge — Transaction Service</description>

    <properties>
        <java.version>17</java.version>
        <!-- ByteBuddy más nuevo, compatible con JVMs recientes (JDK 25);
             evita los warnings de sun.misc.Unsafe del 1.14.13 que trae el parent. -->
        <byte-buddy.version>1.17.6</byte-buddy.version>
    </properties>

    <dependencies>

        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- El proyecto apunta a Java 17. Si se compila/testea sobre un JDK más
             nuevo (ej. 25), Mockito necesita el modo experimental de ByteBuddy y
             la JVM emite warnings de carga dinámica de agentes, CDS y sun.misc.Unsafe.
             Este profile se activa solo en JDK >= 21 y silencia ese ruido; en
             Java 17 el build queda sin flags extra. -->
        <profile>
            <id>jdk-moderno</id>
            <activation>
                <jdk>[21,)</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>
                                -Dnet.bytebuddy.experimental=true
                                -XX:+EnableDynamicAgentLoading
                                -Xshare:off
                                --sun-misc-unsafe-memory-access=allow
                            </argLine>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
